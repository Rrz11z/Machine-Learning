version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - API_HOST=0.0.0.0
      - API_PORT=8000
      - API_RELOAD=false
      - DEBUG=false
    volumes:
      - .:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000
